import { ChangeSpacebarToDividerPipe } from './change-spacebar-to-divider.pipe';

describe('ChangeSpacebarToDividerPipe', () => {
  it('create an instance', () => {
    const pipe = new ChangeSpacebarToDividerPipe();
    expect(pipe).toBeTruthy();
  });
});
